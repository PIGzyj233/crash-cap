# Phase 0 test workspace symbols

Run the pinned `symsorter` helper against a fixture's matching PE and PDB to
populate this directory with the Unified layout. Binary artifacts under this
directory are test inputs and must not be committed.
