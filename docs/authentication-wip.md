# 用户认证实现中断记录

2026-09-07 按用户要求暂停。此提交是 WIP 快照，尚未完成验收，不应直接部署。完整目标见 `docs/authentication.md` 和 ADR-0023。

## 已落地的主要改动

- 本地用户、Argon2id 密码、数据库会话、CSRF、上传令牌、共享限流及管理员初始化命令。
- 注册／登录、账号与令牌管理接口；业务路由认证、上传完成归属检查和令牌权限限制。
- 上传人与审计字段、分组负责人 ID、增量迁移和历史未知用户回填；审核人改为服务端身份。
- Rust CLI 的 CRASHCAP_TOKEN／--token-file 支持与回执归属，平台凭据不传给对象存储。
- 前端登录、个人账号、管理员管理、上传记录、用户筛选及会话恢复逻辑。
- Jira 仅补充设计契约，没有运行时接入。

## 验证结果（记录当次运行，不代表最后一版已全部通过）

- 第一轮认证＋审核＋迁移专项：17 passed。
- PostgreSQL 真实迁移回填和审核测试：6 passed；使用独立临时容器及独立 schema。
- Rust CLI 单元／命令测试通过。
- 前端类型检查曾通过，之后新增上传记录页面和接口，尚未重新检查。
- 后端全量第一轮：381 passed、89 skipped、7 failed；已调整路由清单、认证测试客户端、查询计数及旧审核请求等，尚未重跑全量。
- 前端全量第一轮：93 passed、6 failed；已调整旧审核人输入、身份分区存储键及 fetch 调用断言，尚未重跑。
- 最新认证测试：10 passed、3 failed。失败的 EXE／DLL／PDB 去重归属测试调用了明确禁止验证全局符号的 Fake Core。需要将此测试改为使用显式验证证据测试夹具／验收接口，或准备真实 Core 与 PE/PDB 样本；不要削弱生产验证器以通过测试。DMP 场景通过。

## 继续时优先事项

1. 修复上述三个认证测试，补充浏览器认证／过期恢复／账号切换测试、CI 令牌不泄漏测试及 PostgreSQL 并发注册／限流验证。
2. 重新生成并核对 OpenAPI 和前端类型；检查 mock API 是否覆盖新加的 listUploads。
3. 运行并修复 `ruff check api worker cli tests migrations`、`mypy api worker cli`、`pytest`、前端 lint/test/build、`cargo test -p crashcap`。最新 ruff 仍有若干行长和测试密码 S105 标记待处理；mypy 的唯一返回类型注解错误已修，待重跑。
4. 审阅会话过期时的并发请求、账号切换和上传续传，完善前端行为验证；检查 SSE 长连接的失效处理、指标端点的内部访问约束，以及 Origin 与真实客户端 IP 的部署配置。
5. 更新 `scripts/phase1/target_perimeter_probe.py` 及其测试：目前仍错误地禁止 login/users 身份路由。核对其他历史匿名脚本及原生审核请求兼容调整。
6. 审阅最终差异和部署说明，补齐账号禁用／改密／令牌轮换、审核幂等身份隔离、真实文件内容去重归属的验收证据，再将 WIP 标记移除。

没有运行生产迁移，没有部署或推送远端。现有主分支保持不变。
