{
  "schema_version": "1.0",
  "phase": "Architecture deepening G5",
  "generated_at": "2026-08-24T11:04:18Z",
  "base_commit": "f4ee82eb2482664f2c5ec2d0a848792cfd067b7b",
  "decision": "PASS / G5 local representation compatibility gate",
  "scope": "local FastAPI wire fixtures, generated OpenAPI browser types, production frontend build, and crashcap-ci typed response decoding",
  "checks": [
    {
      "name": "three-wave-wire-baseline-and-activation",
      "status": "PASS",
      "command": "cd platform && uv run --extra dev pytest -q tests/test_http_representations.py",
      "result": "3 passed; success/list cursor/202/error/SSE/download/presigned/Canonical, response-model round trips, operation IDs, schema references, and old/new upload quadrants passed"
    },
    {
      "name": "platform-regression",
      "status": "PASS",
      "command": "cd platform && uv run --extra dev ruff check api worker cli tests migrations && uv run --extra dev mypy api worker cli && uv run --extra dev pytest -q tests migrations/tests",
      "result": "Ruff PASS; mypy PASS for 46 source files; 153 passed, 6 explicitly opt-in service tests skipped"
    },
    {
      "name": "openapi-generation-and-browser",
      "status": "PASS",
      "command": "cd platform/frontend && pnpm openapi:generate && pnpm openapi:check && pnpm test && pnpm build",
      "result": "OpenAPI and generated TypeScript current; 21 frontend tests passed; production Vite build passed; no generated application/json or text/event-stream success response is unknown",
      "openapi_sha256": "337a130103151345d97c53f3e096e95621cbc9f68514c7a63c3e442f63fe94f0",
      "generated_types_sha256": "40de6cc6212e013c2911e4b4c4be6de370c07786266928f1dc32af952eee8791"
    },
    {
      "name": "canonical-single-source-openapi",
      "status": "PASS",
      "result": "CanonicalAnalysisResult and its definitions are mechanically loaded from analysis-result-v1.schema.json with source SHA metadata; analysis, threads, and modules reference those components and no copied Pydantic Canonical model exists"
    },
    {
      "name": "crashcap-ci-typed-decode",
      "status": "PASS",
      "command": "cargo fmt --all --check && cargo clippy -p crashcap-ci --all-targets --locked -- -D warnings && cargo test -p crashcap-ci",
      "result": "Clippy PASS; 23 unit plus 3 CLI integration tests passed; unknown additive fields, missing/type failures, enum failures, old-server part_size fallback, and new-server part_size were exercised"
    },
    {
      "name": "multipart-client-consistency",
      "status": "PASS",
      "result": "Browser and Rust clients both use server multipart.part_size when present, reject zero/invalid sizes, and retain the 64 MiB fallback for an old server"
    }
  ],
  "limitations": [
    "The four compatibility quadrants use checked-in wire fixtures and current source clients; archived historical release binaries and a live old server were not executed.",
    "Frontend verification is TypeScript, Vitest, generated-schema drift, and a production build; no manual browser UAT was performed.",
    "No remote CI, target intranet gateway, RustFS deployment, Linux OCI image, or production traffic observation was exercised.",
    "The Vite build reports an existing large-chunk warning; it is not a representation compatibility failure."
  ]
}
